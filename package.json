{
  "name": "budget-impact-lens-root",
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix frontend\" \"cd backend && source venv/bin/activate && fastapi dev main.py\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}