{
  "name": "budget-impact-lens-root",
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix frontend\" \"cd backend && . venv/bin/activate && fastapi dev main.py\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}