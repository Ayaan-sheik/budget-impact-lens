# Backend API Configuration
# Copy this file to .env.local and update with your actual values

# Production: Your deployed Render backend URL
VITE_API_URL=https://budget-impact-lens.onrender.com

# Development: Local backend
# VITE_API_URL=http://localhost:8000

# Note: For Vite projects, all environment variables must be prefixed with VITE_
# They are exposed to the client-side code via import.meta.env.VITE_*
